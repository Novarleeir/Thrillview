#![allow(non_snake_case)]
#![allow(non_camel_case_types)]
use log::{info, warn, error, debug, trace};
use std::path::{PathBuf};

//For parsing OVL files

fn ovlExists(){
    //check if zap file exists on disk
}

fn ovlLoad(){
    //load zap into memory
}

fn ovlParse(){
    //read through the zap file system and make a vector containing the paths and file information of the ovls inside
}
